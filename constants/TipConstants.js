let keyMirror = require('keymirror');

// These are the event names to be used through out the app. `CALCULATE_TIP` will occur when the "Calcuate" button is pressed.
module.exports = keyMirror({
  CALCULATE_TIP: null
});
